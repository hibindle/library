import styled from 'styled-components'

const IconTextWrap = styled('div')(({}) => ({
	display: `flex`,
	flexDirection: `row`,
	alignItems: `center`,
	textAlign: `center`
}))

export default IconTextWrap
