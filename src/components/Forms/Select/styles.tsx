import styled from 'styled-components'

const SCSelect = styled('select')`
	display: flex;
	position: relative;
`

const SCOption = styled('option')`
	display: flex;
	position: relative;
`

export { SCSelect, SCOption }
